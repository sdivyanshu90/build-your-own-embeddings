# Benchmarks

Run `embedding-project benchmark` or the separately marked performance test and record the
exact artifact manifest, corpus, batch size, Python/package versions, hardware, warmup,
sample count, and latency/throughput distribution. No portable threshold or model-quality
result is checked into this repository because the synthetic model and host-dependent timing
would make such a number misleading.

